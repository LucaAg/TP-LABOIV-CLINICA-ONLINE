import { AgregarBorderDirective } from './agregar-border.directive';

describe('AgregarBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new AgregarBorderDirective();
    expect(directive).toBeTruthy();
  });
});
